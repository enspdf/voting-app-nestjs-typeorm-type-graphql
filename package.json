{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^3.19.0"
    },
    "name": "voting-app",
    "scripts": {
        "build:common": "npx lerna run build --scope='@votingapp/common'",
        "clean": "npx lerna clean && rm -rf node_modules"
    }
}